import {combineReducers} from 'redux';
import navigationMenuItems from '../components/navigation/reducers';

const rootReducer = combineReducers({
  navigationMenuItems
});

export default rootReducer;
